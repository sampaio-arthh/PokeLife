<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokélife - Registrar batalha</title>
    <link rel="stylesheet" href="top.css"> <!-- CSS da navbar e de algumas configurações padronizadas -->
    <link rel="icon" type="image/x-icon" href="icon.png"> <!-- Logo da pokébola na guia -->
</head>
<body>
    <nav> <!-- Cabeçalho padrão do site -->
        <div class="nav-in">
            <a href="/"><h2>PokéLife</h2></a>
            <div class="cont-nav">
                <a href="pokemons"><h3>Pokémons</h3></a>
                <a href="trainers"><h3>Treinadores</h3></a>
                <a href="battles"><h3>Batalhas</h3></a>
            </div>
        </div>
    </nav> <!-- Fim do cabeçalho -->
    <h1>Esperamos que você tenha ganhado !</h1>
    <form method="post" id="battleForm">
        <div class="form-int">
            <div class="l1">
                <label for="selId_treinador1">Treinador 1</label>
                <select name="selId_treinador1" id="selId_treinador1">
                    <% treinadores.forEach((treinador) => { %>
                        <option value="<%= treinador.id_treinador %>"><%= treinador.id_treinador %> - <%= treinador.nome %></option>
                <%  }); %>
                </select>
                <label for="selId_pokemon1">Pokémon 1</label>
                <select name="selId_pokemon1" id="sel_pokemon1">
                    <% pokemons.forEach((pokemon) => { %>
                        <option value="<%= pokemon.id_pokemon %>"><%= pokemon.id_pokemon %> - <%= pokemon.nome %></option>
                <%  }); %>
                </select>

                <label for="selId_treinador2">Treinador 2</label>
                <select name="selId_treinador2" id="selId_treinador2">
                    <% treinadores.forEach((treinador) => { %>
                        <option value="<%= treinador.id_treinador %>"><%= treinador.id_treinador %> - <%= treinador.nome %></option>
                <%  }); %>
                </select>

                <label for="selId_pokemon2">Pokémon 2</label>
                <select name="selId_pokemon2" id="sel_pokemon2">
                    <% pokemons.forEach((pokemon) => { %>
                        <option value="<%= pokemon.id_pokemon %>"><%= pokemon.id_pokemon %> - <%= pokemon.nome %></option>
                <%  }); %>
                </select>
            </div>
            <div class="l2">
                <label for="inputData">Data</label>
                <input type="date" name="inputData" id="inputData" required>

                <label for="selId_treinador_vencedor">Treinador Vencedor</label>
                <select name="selId_treinador_vencedor" id="selId_treinador_vencedor">
                    <% treinadores.forEach((treinador) => { %>
                        <option value="<%= treinador.id_treinador %>"><%= treinador.id_treinador %> - <%= treinador.nome %></option>
                <%  }); %>
                </select>
                <button type="submit" onclick="submitForm('/addBattle')">Enviar</button>
            </div>
        </div>
    </form>
    <script>
        function submitForm(action) {
            const form = document.getElementById('battleForm');
            form.action = action;
            form.submit();
        }
    </script>
</body>
<style>
    h1{
        text-align: center;
        font-family: "Nunito", monospace;
        font-size: 24px;
        font-weight: 700;
        margin-top: 2.5vh;
    }
    form{
        width: 100vw;
        display: flex;
        justify-content: center;
        margin: 2.5vh 0;
    }
    .form-int{
        width: 50vw;
        height: 40vh;
        background-color: rgb(51, 146, 255);
        border-radius: 5px;
        box-shadow: rgba(17, 17, 26, 0.1) 0px 4px 16px, rgba(17, 17, 26, 0.1) 0px 8px 24px, rgba(17, 17, 26, 0.1) 0px 16px 56px;
    }
    label{
        font-size: 20px;
    }
</style>
</html>